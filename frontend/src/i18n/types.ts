export type Locale = "ar" | "fr" | "en";

export type ReviewMessage = { name: string; city: string; text: string; rating: number };
export type FaqItem = { q: string; a: string };
export type WhatsAppProof = { name: string; city: string; text: string };
